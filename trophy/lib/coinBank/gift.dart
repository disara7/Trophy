import 'package:flutter/material.dart';

class GiftPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Gift Page'),
      ),
      body: Center(
        child: Text('This is the Gift Page'),
      ),
    );
  }
}
