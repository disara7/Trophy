import 'package:flutter/material.dart';

class CoinsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Coins Page'),
      ),
      body: Center(
        child: Text('This is the Coins Page'),
      ),
    );
  }
}
