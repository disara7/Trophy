import 'package:flutter/material.dart';

class SpinPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Spin Page'),
      ),
      body: Center(
        child: Text('This is the Spin Page'),
      ),
    );
  }
}
