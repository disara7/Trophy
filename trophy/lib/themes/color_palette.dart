import 'package:flutter/material.dart';

class Palette {
  static const MaterialColor appOrange = const MaterialColor(
    0xFFf89026, // 0% comes in here, this will be color picked if no shade is selected when defining a Color property which doesn’t require a swatch.
    const <int, Color>{
      50: const Color(0xFFf89026), //10%
      100: const Color(0xFFf89026), //20%
      200: const Color(0xFFf89026), //30%
      300: const Color(0xFFf89026), //40%
      400: const Color(0xFFf89026), //50%
      500: const Color(0xFFf89026), //60%
      600: const Color(0xFFf89026), //70%
      700: const Color(0xFFf89026), //80%
      800: const Color(0xFFf89026), //90%
      900: const Color(0xFFf89026), //100%
    },
  );
  static const MaterialColor appBlack = const MaterialColor(
    0xFF222222, // 0% comes in here, this will be color picked if no shade is selected when defining a Color property which doesn’t require a swatch.
    const <int, Color>{
      50: const Color(0xFF222222), //10%
      100: const Color(0xFF222222), //20%
      200: const Color(0xFF222222), //30%
      300: const Color(0xFF222222), //40%
      400: const Color(0xFF222222), //50%
      500: const Color(0xFF222222), //60%
      600: const Color(0xFF222222), //70%
      700: const Color(0xFF222222), //80%
      800: const Color(0xFF222222), //90%
      900: const Color(0xFF222222), //100%
    },
  );
  static const MaterialColor appGray = const MaterialColor(
    0xFFded9d9, // 0% comes in here, this will be color picked if no shade is selected when defining a Color property which doesn’t require a swatch.
    const <int, Color>{
      50: const Color(0xFFded9d9), //10%
      100: const Color(0xFFded9d9), //20%
      200: const Color(0xFFded9d9), //30%
      300: const Color(0xFFded9d9), //40%
      400: const Color(0xFFded9d9), //50%
      500: const Color(0xFFded9d9), //60%
      600: const Color(0xFFded9d9), //70%
      700: const Color(0xFFded9d9), //80%
      800: const Color(0xFFded9d9), //90%
      900: const Color(0xFFded9d9), //100%
    },
  );
  static const MaterialColor appBrown = const MaterialColor(
    0xFF933433, // 0% comes in here, this will be color picked if no shade is selected when defining a Color property which doesn’t require a swatch.
    const <int, Color>{
      50: const Color(0xFF933433), //10%
      100: const Color(0xFF933433), //20%
      200: const Color(0xFF933433), //30%
      300: const Color(0xFF933433), //40%
      400: const Color(0xFF933433), //50%
      500: const Color(0xFF933433), //60%
      600: const Color(0xFF933433), //70%
      700: const Color(0xFF933433), //80%
      800: const Color(0xFF933433), //90%
      900: const Color(0xFF933433), //100%
    },
  );
}
